"""Athena Autonomy Layer — always-on event loop for autonomous trading.

Replaces ephemeral Claude Code sessions with a persistent Python process
that monitors, decides, executes, and logs every step with full provenance.
"""
